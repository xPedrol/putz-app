export enum DialogAction {
  DELETE = 'DELETE',
  UPDATE = 'UPDATE',
  POST = 'POST'
}

export const dialogActions: DialogAction[] = [
  DialogAction.DELETE,
  DialogAction.POST,
  DialogAction.UPDATE
];
